using UnityEngine;

public class DiceSpawner : MonoBehaviour
{
    public GameObject dicePrefab;

    /*void Start()
    {
        if (!dicePrefab)
        {
            Debug.LogError("DiceSpawner: Assign dicePrefab in the Inspector.");
            return;
        }

        Instantiate(dicePrefab, Vector3.zero, Quaternion.identity);
    }*/
}